<div class="navbar bg-base-100 align-middle dark:bg-black">
    <div class="navbar-start">
      <div class="dropdown">
        <label tabindex="0" class="btn btn-ghost lg:hidden">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h8m-8 6h16" /></svg>
        </label>
        <ul tabindex="0" class="menu menu-compact dropdown-content mt-3 p-2 shadow bg-base-100 rounded-box w-52">
            <li> <a href="/profile" class="btn btn-ghost uppercase dark:text-slate-200">Profile</a></li>
            <% if (favoriteProducts.length > 0) { %>
               <li><a href="#favorites" class="btn btn-ghost uppercase dark:text-slate-200">Favorites</a></li>
            <% } %>
            <li> <a href="/logout" class="btn btn-ghost uppercase dark:text-slate-200">Logout</a></li>
        </ul>
      </div>
      <a href="/" class="btn btn-ghost uppercase text-3xl md:text-4xl dark:text-slate-300 font-rampart-one">PriceKicks</a>
    </div>
    <div class="navbar-center hidden lg:flex">
      <ul class="menu menu-horizontal p-0">
        <li> <a href="/profile" class="btn btn-ghost uppercase dark:text-slate-200">Profile</a></li>
        <% if (favoriteProducts.length > 0) { %>
            <li><a href="#favorites" class="btn btn-ghost uppercase dark:text-slate-200">Favorites</a></li>
        <% } %>
      </ul>
    </div>
    <div class="navbar-end hidden md:flex">
        <button class="btn btn-ghost btn-square btn-outline"><a href="/logout" class="dark:text-slate-200">Log out</a></button>
    </div>
  </div>